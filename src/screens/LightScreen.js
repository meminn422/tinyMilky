import React from "react-native";
import { View, Text } from "react-native";

const LightScreen = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>This is light page</Text>
        </View>
    );
};

export default LightScreen;